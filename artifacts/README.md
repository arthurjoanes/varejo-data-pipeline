# Saídas locais

`demo` e `scripts/benchmark.py` geram relatórios HTML e arquivos JSON nesta pasta.
Ela começa vazia em um clone: nenhuma execução anterior é necessária.
Saídas operacionais não são versionadas. Capturas e medições de referência
estão em `docs/images` e `docs/evidence`.
